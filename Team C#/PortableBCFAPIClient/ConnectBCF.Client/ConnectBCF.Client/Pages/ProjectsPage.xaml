<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Name="page" x:Class="ConnectBCF.Client.Pages.ProjectsPage" Title="Projects">
  <ListView x:Name="ProjectList" ItemsSource="{Binding Projects}" ItemSelected="OnProjectSelected" SelectedItem="{Binding SelectedProject, Mode=TwoWay}">
    <ListView.ItemTemplate>
      <DataTemplate>
        <TextCell Text="{Binding Name}" Detail="{Binding ProjectId}" DetailColor="Gray">
          <TextCell.ContextActions>
            <MenuItem Text="Delete" IsDestructive="True" Command="{Binding BindingContext.DeleteProjectCommand, Source={x:Reference page}}" CommandParameter="{Binding .}"/>
          </TextCell.ContextActions>
        </TextCell>
      </DataTemplate>
    </ListView.ItemTemplate>
  </ListView>
</ContentPage>